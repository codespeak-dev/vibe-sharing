/**
 * Browser-level localStorage persistence — covered by
 * tests/playwright/19-internal-uploads-preference.spec.ts.
 */
import { describe, test } from "vitest";

describe("Feature: Internal Uploads Preference Persistence", () => {
  test.skip("Save and restore 'Show internal uploads' preference when enabled: see playwright spec");
  test.skip("Save and restore 'Show internal uploads' preference when disabled: see playwright spec");
});
